bin/
obj/
*.user
*.suo
.vs/
*.lock.json
packages/
F1SimHubLive.Settings.json

# Local backups and session artifacts
*.bak
*.bak-*
*.preCarNumbers-*
*.preSignature-*
*.preSigResize-*
*.preFlagStatus-*
*.preFlagFix-*
*.preLayerFix-*
*.preF1FlagSync-*

# Installer publish output
installer/**/publish/
installer/**/*.pdb

# OS clutter
Thumbs.db
.DS_Store

# Local-only signing runbook — contains Azure resource IDs and recovery commands.
# Not secret per se (real secrets live in Azure HSM + GitHub Secrets vault), but keep on local disk only.
.signing-runbook.md
