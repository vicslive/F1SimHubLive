<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net48</TargetFramework>
    <RootNamespace>F1SimHubLive</RootNamespace>
    <AssemblyName>F1SimHubLive</AssemblyName>
    <LangVersion>10</LangVersion>
    <Nullable>enable</Nullable>
    <AppendTargetFrameworkToOutputPath>false</AppendTargetFrameworkToOutputPath>
    <SimHubPath Condition="'$(SimHubPath)' == ''">$(MSBuildProgramFiles32)\SimHub</SimHubPath>
    <SimHubLibFallback>$(MSBuildProjectDirectory)\lib\simhub</SimHubLibFallback>
    <Version>1.3.5</Version>
    <AssemblyVersion>1.3.5.0</AssemblyVersion>
    <FileVersion>1.3.5.0</FileVersion>
    <InformationalVersion>1.3.5</InformationalVersion>
    <Product>F1SimHubLive</Product>
    <Company>vicslive</Company>
    <Copyright>Copyright Ã‚Â© 2026 Victor de Souza</Copyright>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.AspNet.SignalR.Client" Version="2.4.3" />
    <PackageReference Include="Newtonsoft.Json" Version="13.0.3" />
  </ItemGroup>

  <!--
    SimHub DLLs are referenced from a live SimHub install when present
    (developer machines where the user has SimHub installed), and fall back
    to the vendored copies under lib/simhub/ when not (CI runners, fresh
    clones, anywhere SimHub isn't installed). The vendored copies exist
    purely to make CI builds work — see lib/simhub/README.md for the
    licensing rationale. <Private>false</Private> keeps them OUT of the
    plugin build output: SimHub already has its own copies at runtime, we
    don't want to overwrite or shadow them.
  -->
  <ItemGroup>
    <Reference Include="SimHub.Plugins">
      <HintPath Condition="Exists('$(SimHubPath)\SimHub.Plugins.dll')">$(SimHubPath)\SimHub.Plugins.dll</HintPath>
      <HintPath Condition="!Exists('$(SimHubPath)\SimHub.Plugins.dll')">$(SimHubLibFallback)\SimHub.Plugins.dll</HintPath>
      <Private>false</Private>
    </Reference>
    <Reference Include="GameReaderCommon">
      <HintPath Condition="Exists('$(SimHubPath)\GameReaderCommon.dll')">$(SimHubPath)\GameReaderCommon.dll</HintPath>
      <HintPath Condition="!Exists('$(SimHubPath)\GameReaderCommon.dll')">$(SimHubLibFallback)\GameReaderCommon.dll</HintPath>
      <Private>false</Private>
    </Reference>
    <Reference Include="log4net">
      <HintPath Condition="Exists('$(SimHubPath)\log4net.dll')">$(SimHubPath)\log4net.dll</HintPath>
      <HintPath Condition="!Exists('$(SimHubPath)\log4net.dll')">$(SimHubLibFallback)\log4net.dll</HintPath>
      <Private>false</Private>
    </Reference>
    <Reference Include="WindowsBase" />
    <Reference Include="PresentationCore" />
    <Reference Include="PresentationFramework" />
    <Reference Include="System.Xaml" />
  </ItemGroup>

  <ItemGroup>
    <Compile Remove="installer\**" />
    <EmbeddedResource Remove="installer\**" />
    <None Remove="installer\**" />
    <Compile Remove="picker\**" />
    <EmbeddedResource Remove="picker\**" />
    <None Remove="picker\**" />
    <Compile Remove="scripts\**" />
    <None Remove="scripts\**" />
    <Compile Remove="dashboards\**" />
    <None Remove="dashboards\**" />
  </ItemGroup>

  <!--
    Auto-deploy the plugin + dashboards into the local SimHub install after a
    successful build. Skips gracefully when SimHub is running, when SimHub is
    not installed, or when -p:DeploySimHub=false is passed (CI sets this).

    Pass -p:StartSimHub=true to auto-launch SimHubWPF.exe after a successful
    deploy (handy for dev iteration: close SimHub, build, re-launch).
  -->
  <PropertyGroup>
    <DeploySimHub Condition="'$(DeploySimHub)' == ''">true</DeploySimHub>
    <StartSimHub Condition="'$(StartSimHub)' == ''">false</StartSimHub>
  </PropertyGroup>
  <Target Name="DeployToSimHub" AfterTargets="Build" Condition="'$(DeploySimHub)' == 'true'">
    <PropertyGroup>
      <_DeployStartFlag Condition="'$(StartSimHub)' == 'true'"> -StartSimHub</_DeployStartFlag>
    </PropertyGroup>
    <Exec Command="powershell -NoProfile -ExecutionPolicy Bypass -File &quot;$(MSBuildProjectDirectory)\scripts\deploy.ps1&quot; -DllPath &quot;$(TargetPath)&quot; -ProjectRoot &quot;$(MSBuildProjectDirectory)&quot;$(_DeployStartFlag)"
          IgnoreExitCode="true"
          ContinueOnError="WarnAndContinue" />
  </Target>

</Project>
